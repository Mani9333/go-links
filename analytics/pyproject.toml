[project]
name = "go-links-analytics"
version = "1.0.0"
description = "Read-only usage analytics for go/ links (FastAPI + MongoDB)."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"
